# HDU Library Web UI

React frontend for the HDU library booking app.

## Run

Start the Python API from the repository root:

```bash
python -m hdu_library_autobook.web_api
```

Start the frontend:

```bash
cd hdu_library_autobook/web
npm install
npm run dev
```

Open `http://127.0.0.1:5173`.
